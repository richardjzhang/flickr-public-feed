{
  "name": "flickr-public-feed",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.20.0",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "yarn install --only=dev && yarn install && yarn build"
  }
}
